# Phase 7 hardware evidence — run0065 — 2026-08-26

This is the tracked, location-redacted summary of the first Phase 7
instrumentation session. Raw evidence is stored locally in the git-ignored
`hardware-results/private/` directory.

## Build identity

- Firmware semantic version: v0.6.8
- Reported build revision: `cb7aed3-dirty`
- Tested run0065 binary: pre-route-leak-fix instrumentation build,
  50348B static RAM and 972209B flash
- The pre-fix binary SHA-256 was not retained before rebuilding. Because the
  tree was dirty, `cb7aed3-dirty` is not by itself a unique artifact ID.
- Replacement route-leak-fix binary reported flashed by the operator after
  this test: 50348B static RAM, 972233B flash, SHA-256
  `ad796d326178302bcda99517b61ad0faf586c41acec80063b172d0418b4a2c5b`
- Replacement binary hardware retest: pending

## Workload and data-path result

- Active profile: MeshCore, with real ambient traffic.
- WiFi-off baseline ran for 10 minutes before AP testing.
- The full captured run reached 174 received and 174 logged rows.
- `queue_drop`, `bus_miss`, and `row_drop` remained 0.
- One radio CRC error and one bad NMEA checksum occurred during the extended
  run; neither indicated a queue/logging failure.
- Worst recorded SD flush was 54ms; worst health-row session was 47ms.
- GPS remained fixed. Exact positions are intentionally omitted here.

## Heap and fragmentation stages

| Stage | Free heap | Minimum heap | Largest block | Free/allocated blocks |
|---|---:|---:|---:|---:|
| Boot row, tasks not all started | 281092B | 281036B | 262132B | 7 / 153 |
| Settled WiFi-off | 233952B | 229372B | 221172B | 4 / 177 |
| First AP on, no deliberate client | 177728B | 173292B | 167924B | 3 / 359 |
| Client associated | 175372B | 151368B | 163828B | 6 / 372 |
| Browser/session download row | 171124B | 151368B | 155636B | 16 / 404 |
| First warm WiFi-off plateau | 215508B | 151368B | 159732B | 25 / 308 before cycle 2 |
| Second AP on | 175920B | 151368B | 159732B | 9 / 386 |
| Second warm WiFi-off plateau | 214792B | 151368B | 159732B | 29 / 321 immediately after stop |

Interpretation:

- The first AP start cost about 56KB while active.
- The cold-to-first-warm off-state difference was 18444B. Inspection of the
  bundled Arduino-ESP32 framework showed one-time TCP/IP event-task and
  network-interface initialization retained for reuse; treat the first warm
  off state as the lifecycle baseline rather than labeling `heap_min` alone
  as a leak.
- Cycle 2 lost another 716B and added 13 allocated blocks. `startAp()` was
  calling `registerRoutes()` on every cycle, while `WebServer::stop()` keeps
  its request-handler list. Route registration was made idempotent after this
  run. Ten fixed-build cycles are required to confirm the diagnosis.
- Browser/download current heap partially recovered from about 170.8KB to
  174.2KB while the client remained associated. A disconnect and AP stop were
  needed for the lifecycle comparison; the full repeated-download matrix was
  not completed.

## Task stack evidence and provisional review targets

Watermarks are lifetime minimum unused bytes, directly reported in bytes by
this ESP32 FreeRTOS port. These are review candidates, not approved changes.

| Task | Current allocation | Minimum unused | Unused % | Provisional target | Potential saving | Required evidence before change |
|---|---:|---:|---:|---:|---:|---|
| Radio | 4096B | 2132B | 52.1% | 3072B | 1024B | Profile switch/pause path plus combined-load repeat |
| GPS | 3072B | 1492B | 48.6% | 2816B | 256B | Combined load and soak |
| Logger | 5120B | 1780B | 34.8% | 4608B | Large run, repeated health rows/download contention |
| UI | 4096B | 2020B | 49.3% | 3584B | Every menu level, slider, toast, dim, and profile UI |
| WiFi | 8192B | 3188B | 38.9% | 7168B | Full five-download/settings workload after leak fix |

The provisional targets would save 3328B total and still satisfy the Phase 7
rule using this run's observed usage: at least 25% and 1KB headroom, whichever
is larger. They must not be applied as a batch. Change one task at a time only
after its missing workload is measured, then repeat the affected hardware
stages.

## Deferred continuation

1. Confirm the flashed replacement artifact using the SHA-256 above (the
   run0069 monitor attached after boot, so this remains an evidence gap).
2. Complete the UI interaction workload before changing UI stack size.
3. Complete repeated CSV downloads and settings saves before changing WiFi
   stack size.
4. Right-size one task at a time, beginning with the best-supported candidate,
   and repeat its affected matrix stages.
5. Finish the 800-detection/30-minute combined load and two-hour soak.

## Replacement route-fix retest — run0069

The route-registration fix was retested on the same Cardputer/SD setup after
the first session. The serial monitor attached after boot, so the boot banner
and flashed artifact hash are not part of this capture; the device reported
`run=69` throughout.

- Ten complete WiFi AP on/off cycles were completed without a client.
- Warm WiFi-off current heap settled at 217,304–217,592 B (217,448 B at the
  final settled sample); largest block stayed 167,924 B.
- Warm-off block counts stayed at 20 free / 274–277 allocated, with no
  cumulative allocation trend like the pre-fix run0065 result.
- WiFi-on heap settled around 178.1–178.8 KB; `qdrop`, `busmiss`, and
  `rowdrop` remained 0 throughout. The run ended with 139 received and 139
  logged rows; `crcerr=0` in the retest interval.

**Baseline C: PASS.** The retained serial capture is in the local ignored
`hardware-results/private/2026-08-26-phase7-route-fix-serial.log` file.

## Browser workload follow-up — run0069

The UI/AP client workload was then run on the same device. Ten CSV transfer
checkpoint completions were captured (detections and session downloads), and
profile settings writes for MeshCore and Meshtastic were confirmed.

- WiFi-on current heap settled around 170 KB; the lowest observed current
  heap was 163,724 B and the largest block reached 147,444 B during a
  transfer. `heap_min` reached 89,400 B.
- The first post-stop sample (about 90 seconds) was 211,772 B free,
  159,732 B largest block, and 27/328 blocks. Before browser traffic the same
  run had settled at 217,448 B, 167,924 B, and 20/276 blocks. A later run0070
  control continued recovering several KB during its idle tail, so this
  5,676 B / 52-block difference is not yet proven persistent.
- `queue_drop`, `bus_miss`, and `row_drop` stayed 0. One client reset caused
  socket-write errors during a transfer; the remaining `request handler not
  found`/zero-content warnings coincide with the dynamic CSV `onNotFound`
  handler and need a client-side success check.

**Baseline D: NOT YET ACCEPTED.** The transfer/settings workload ran, but the
post-workload heap/largest-block recovery window was too short to distinguish
delayed WiFi teardown from a persistent request/client cost. Repeat with a
longer off-state settle after the P2 controls.
